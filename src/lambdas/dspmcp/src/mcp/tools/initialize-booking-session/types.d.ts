/**
 * Type definitions for initialize-booking-session tool
 *
 * This tool doesn't have specific response types beyond the session token in headers.
 * Response types are handled by the serializer.
 */

// No tool-specific types needed for this tool
// All types are defined in the parent types.d.ts or schema
